# Standalone tool: do not interact with the parent monorepo workspace.
ignore-workspace-root-check=true
# Never run dependency install scripts unless explicitly allowlisted.
only-built-dependencies=[]
